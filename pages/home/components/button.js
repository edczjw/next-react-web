// import 'antd/dist/antd.css'
// import { Button } from 'antd'
import React from 'react'

export default ({ children }) => {
    return (
        <button>
            {children}
        </button>
    )
}